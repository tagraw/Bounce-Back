import React from 'react';
import { ScrollView } from 'react-native';
import { SelectGroup } from '../../services/selectgroup';

export default function SignupScreen() {
  return (
    <ScrollView>
      <SelectGroup/>
    </ScrollView>
  );
}